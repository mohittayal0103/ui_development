---Page 4---
In javascript variable length is upto memory size. 

What is memory size?
- It depends on the which browser you are using your code basically its different for sifferent browser. 
however it mostly lies between 2mb to 10mb


What is the diff bw foreach map and filter?

This is called listing function in javascript.
it is specially designed for array purpose, we can read and print the data from array.
if ia m writing only js in that case there is no diff bw all three functions
but in frameworks they differ by there uses.